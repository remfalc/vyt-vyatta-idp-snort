multi:

type: txt

syntax:expression: pattern $VAR(@) "^[0-9]+\:?[0-9]*$" ; "invalid gid:sid \"$VAR(@)\""

help: Snort ID (sid) to enable [DEPRECATED]

val_help: [<gid>:]<sid>; Sid to enable (gid defaults to 1)
