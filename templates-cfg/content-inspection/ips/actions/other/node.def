type: txt

default: "pass"

syntax:expression: $VAR(@) in "drop", "sdrop", "alert", "pass";
                   "Action must be \"drop\", \"sdrop\", \"alert\", or \"pass\"."

help: IPS action for packets matching other rules [REQUIRED]

val_help: drop; Drop the packet and generate an alert
val_help: sdrop; Silently drop the packet
val_help: alert; Generate an alert and let the packet pass  
val_help: pass; Let the packet pass and do nothing (default)
