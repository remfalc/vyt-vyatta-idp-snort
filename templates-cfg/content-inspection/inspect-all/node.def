help: Policy to inspect all inbound, outbound and forwarded traffic

# in future, this might check that either enable or ipv6-enable has been set
commit:expression: $VAR(./enable) != ""; "Need to set 'enable' to inspect all IPV4 traffic"
